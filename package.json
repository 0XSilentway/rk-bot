{
  "name": "rk-bot",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "inspect": "bun run src/tools/inspect.ts",
    "replay": "bun run src/tools/replay.ts",
    "skills": "bun run src/tools/skills.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  }
}
