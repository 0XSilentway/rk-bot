# ================================================================
# rk-bot main config
# ================================================================
# Format: <key> <value>
# Lines starting with # are comments.
# Save the file — the bot hot-reloads automatically.
# ================================================================

######## General ########
# 0 = bot paused,  1 = bot running
enabled 1

# AI loop poll interval (ms)
tickMs 500


######## Combat ########
# Cast range in tiles (Bolt spells = 9)
attackDistance 9

# Melee range for basic attack (skill=0 in mon_control.txt)
attackMeleeDistance 2

# Stop-short (walk to attackDistance - approachStopShort)
attackApproachStopShort 2

# Min ms between move commands (throttle path spam)
attackMoveDebounce 400

# Min ms between skill casts (bolt cast time + safety)
attackCastDebounce 2500


######## Emergency wing ########
# 1 = use direct 0x40 teleport packet (free, no item)
# 0 = use Fly Wing item (0x2F, consumes item)
useTeleportPacket 1

# Fly Wing item ID (only used if useTeleportPacket 0). RayRag = 601
flyWingItemID 601

# Wing when own HP % drops below this (safety net)
teleportOnHPPct 30

# Min ms between two wings
wingCooldownMs 4000

# Sight range for teleport=2 rules (tiles)
teleportSightRange 12


######## Home / Kafra (auto-return when inventory full) ########
# Where to warp when inventory fills up (usually near your Kafra Staff)
homeMap prt_fild08
homeX 159
homeY 362

# Where to warp back to after storage (your farm spot)
farmMap moc_fld17
farmX 250
farmY 210

# 1 = auto-warp back to farm after user resume
# 0 = stay at home, user warps back manually
autoReturnAfterResume 1


######## Loot ########
# 1 = pick up every drop within range, 0 = pick nothing
lootAll 1

# Max age of a drop before we forget it (ms)
lootMaxAgeMs 30000

# Max range to pick up a drop (tiles)
lootRange 12


######## Roam ########
# 0 = stand still when no target, 1 = wander to random tile nearby
roamAuto 0

# Idle time before roaming (ms)
roamIdleMs 4000

# Random walk radius from current pos (tiles)
roamRadius 15
